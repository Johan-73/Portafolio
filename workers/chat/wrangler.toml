name = "chat-johan"
main = "src/index.ts"
compatibility_date = "2026-05-01"
workers_dev = true

[vars]
ALLOWED_ORIGIN = "https://johan-73.github.io"
PER_IP_DAILY = "5"
GLOBAL_DAILY = "200"

# Secrets set via `wrangler secret put` in production:
#   OPENAI_API_KEY
#   UPSTASH_REDIS_REST_URL
#   UPSTASH_REDIS_REST_TOKEN
# For local dev, these are read from .dev.vars (gitignored)
